import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.highlightingstatenisland.app',
  appName: 'Highlighting Staten Island',
  webDir: 'public'
};

export default config;
